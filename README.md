gophpdoc
========

gophpdoc is a tool like `godoc.org` which can fetch code from remote repositories and display the (auto-generated) documentation live!
gophpdoc uses phpdoc (https://www.phpdoc.org/) to generate docs.

This project is still in an early stage of development.

